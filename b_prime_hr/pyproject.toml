[project]
name = "b_prime_hr"
authors = [
    { name = "B-Prime", email = "bhavyaji@gmail.com"}
]
description = "B-Prime HR offline check-in, checkout, and employee activity page for ERPNext/Frappe v15"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
